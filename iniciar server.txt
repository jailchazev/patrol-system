cd /c/patrol-system
source venv/Scripts/actívate
pip install -r requirements.txt
python wsgi.py
http://localhost:5000
http://127.0.0.1:5000

# 1. Ir a la carpeta correcta (por si acaso)
cd /c/security-pwa

# 2. Crear el entorno virtual (se ejecuta UNA sola vez)
python -m venv venv

# 3. Activarlo en Git Bash
source venv/Scripts/activate

# ✅ Verificación: ahora debes ver "(venv)" al inicio de tu terminal
# Ejemplo: (venv) EQUIPO@DESKTOP-GKSA566 MINGW64 /c/security-pwa

# 4. Instalar dependencias
pip install -r requirements.txt

# 5. Ejecutar el servidor
python wsgi.py